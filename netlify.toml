[build]
  command = "npm run build"
  publish = "dist"

# Redireciona SPA para index.html (exceto /api que vai para functions)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
